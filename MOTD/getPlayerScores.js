module.exports = {
	getPlayerScores: async function (client) {
		console.log(client);

		// Grab scores of the players at the end of the quali phase
		// Let players invite themselves after comparison and leaderboard or make it open but
		// people that aren't supposed to be there will be kicked
	}
};